# Assumptions Log

> TradingView MCP Security Mitigations · (no issue — repo issues disabled) · updated 2026-08-08

## Log

One row per assumption, updated in place. IDs: two-letter phase prefix + sequence
(DP-1, RE-1, RS-1, IA-1, PL-1) or task number (1.1, 2.3).

| ID | Phase/Task | Category | Risk | Assumption — rationale | Resolution | Outcome |
|----|------------|----------|------|------------------------|------------|---------|
| DP-1 | Design Philosophy | Problem Interpretation | M | RESOLVED — neither "remove" nor "env-gate" as originally framed. Decision: remove the `ui_evaluate` wildcard and replace it with a **capability-allowlist model** — discrete, explicitly-declared MCP tools for specific operations that were previously reachable via carte-blanche page JS. Each capability is human-approved as a requirement, allowlist-controlled, and extensible at the point of need via an agent-proposal → human-approval → implementation → PR flow (see design philosophy doc). Threat model: enforcement lives in the tool registrar (agent can't bypass), NOT in an in-page JS sandbox (leaky). | User (stakeholder, elicitation interview 2026-08-08) | Resolved — supersedes remove-vs-gate framing |
| DP-2 | Design Philosophy | Problem Interpretation | M | `tv_update` remains an MCP tool vs becoming CLI-only — plan flags both options; tool form preserves remote operability | User (elicitation interview, 2026-08-08) | Confirmed: keep `tv_update` as an MCP tool, as a hardened allowlist capability — one discrete allowlist entry, gated off by default, enforced via `TV_UPDATE_TOKEN` + origin-URL allowlist + signed-tag/pinned-SHA-only fast-forward + fail-closed `npm ci`. Consistent with the DP-1 capability-allowlist model (registrar-enforced, human-approved) |
| DP-3 | Design Philosophy | Complexity Assessment | M | `DANGEROUS_TOOLS` membership — plan's "plus optionally" phrasing leaves the extended set { `tv_launch`, `alert_delete`, `draw_clear`, `batch_run` } undecided | User (elicitation interview, 2026-08-08) | Confirmed: gate ALL FOUR — `tv_launch`, `alert_delete`, `draw_clear`, `batch_run` all become gated allowlist entries (off by default, registrar-enforced, human opt-in), alongside `tv_update` (DP-2) and replacing `ui_evaluate` (DP-1 capability-allowlist model). Blast radius per tool: tv_launch = process spawn/kill; alert_delete/draw_clear = irreversible chart-state mutation; batch_run = fan-out amplification |
| DP-4 | Design Philosophy | Complexity Assessment | L | Complex classification and full path hold — the plan's 9 finding areas with open trade-offs justify elicitation + research; user confirmed at checkpoint | User (classification-and-path-confirmed) | Confirmed |
| DP-5 | Design Philosophy | Workflow Path | L | GitHub Issues being disabled is accepted for this work package — PR #1 + planning folder carry coordination; no tracker linkage will exist | Code: `gh issue list` → "repository has disabled issues" | Validated |
| RE-1 | Requirements Elicitation | Scope Boundaries | M | All 9 mitigation-plan finding areas are in scope for this work package — the plan is the agreed mitigation set and no finding area was explicitly excluded during elicitation | User (mitigation plan approved as the work-package scope) | Confirmed |
| RE-2 | Requirements Elicitation | Implicit Requirements | M | Backward compatibility is preserved where feasible — mitigations default to opt-in env gates rather than breaking the existing read/control surface; the useful read surface (OHLCV, quote, study values, pine reads) stays always-on | User (design-philosophy constraints: "backward-compatible where feasible; preserve the useful read/control surface") | Confirmed |
| RE-3 | Requirements Elicitation | Success Criteria Interpretation | L | Success is verified primarily by the existing `node --test` suite plus new unit tests for each gate/guard (the `_deps` injection points make them unit-testable); no live-CDP e2e is required to call the mitigation done | Code: `tests/{update,launch,sanitization}.test.js` exist; `_deps` injection present in `core/update.js`, `core/health.js` (guards unit-testable without CDP) | Validated |
| RE-4 | Requirements Elicitation | Implicit Requirements | L | Stakeholder discussion was skipped by the user in favor of agent-led elicitation — requirements derive from the security audit + mitigation plan + the three DP decisions, not from a separate stakeholder transcript | User (stakeholder-transcript checkpoint → skip-discussion) | Confirmed |

## Wrap-Up

9 assumptions — all validated/confirmed. The three design-phase open questions (DP-1/2/3) were resolved by user decision during elicitation into a single coherent model: a registrar-enforced capability allowlist. One takeaway: the dangerous-tool surface converged on one uniform rule — power tools are gated allowlist entries (off by default, human opt-in), read tools stay always-on.
