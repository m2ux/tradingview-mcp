# Assumptions Log

> TradingView MCP Security Mitigations · (no issue — repo issues disabled) · updated 2026-08-08

## Log

One row per assumption, updated in place. IDs: two-letter phase prefix + sequence
(DP-1, RE-1, RS-1, IA-1, PL-1) or task number (1.1, 2.3).

| ID | Phase/Task | Category | Risk | Assumption — rationale | Resolution | Outcome |
|----|------------|----------|------|------------------------|------------|---------|
| DP-1 | Design Philosophy | Problem Interpretation | M | RESOLVED — neither "remove" nor "env-gate" as originally framed. Decision: remove the `ui_evaluate` wildcard and replace it with a **capability-allowlist model** — discrete, explicitly-declared MCP tools for specific operations that were previously reachable via carte-blanche page JS. Each capability is human-approved as a requirement, allowlist-controlled, and extensible at the point of need via an agent-proposal → human-approval → implementation → PR flow (see design philosophy doc). Threat model: enforcement lives in the tool registrar (agent can't bypass), NOT in an in-page JS sandbox (leaky). | User (stakeholder, elicitation interview 2026-08-08) | Resolved — supersedes remove-vs-gate framing |
| DP-2 | Design Philosophy | Problem Interpretation | M | `tv_update` remains an MCP tool vs becoming CLI-only — plan flags both options; tool form preserves remote operability | User (elicitation interview, 2026-08-08) | Confirmed: keep `tv_update` as an MCP tool, as a hardened allowlist capability — one discrete allowlist entry, gated off by default, enforced via `TV_UPDATE_TOKEN` + origin-URL allowlist + signed-tag/pinned-SHA-only fast-forward + fail-closed `npm ci`. Consistent with the DP-1 capability-allowlist model (registrar-enforced, human-approved) |
| DP-3 | Design Philosophy | Complexity Assessment | M | `DANGEROUS_TOOLS` membership — plan's "plus optionally" phrasing leaves the extended set { `tv_launch`, `alert_delete`, `draw_clear`, `batch_run` } undecided | User (elicitation interview, 2026-08-08) | Confirmed: gate ALL FOUR — `tv_launch`, `alert_delete`, `draw_clear`, `batch_run` all become gated allowlist entries (off by default, registrar-enforced, human opt-in), alongside `tv_update` (DP-2) and replacing `ui_evaluate` (DP-1 capability-allowlist model). Blast radius per tool: tv_launch = process spawn/kill; alert_delete/draw_clear = irreversible chart-state mutation; batch_run = fan-out amplification |
| DP-4 | Design Philosophy | Complexity Assessment | L | Complex classification and full path hold — the plan's 9 finding areas with open trade-offs justify elicitation + research; user confirmed at checkpoint | User (classification-and-path-confirmed) | Confirmed |
| DP-5 | Design Philosophy | Workflow Path | L | GitHub Issues being disabled is accepted for this work package — PR #1 + planning folder carry coordination; no tracker linkage will exist | Code: `gh issue list` → "repository has disabled issues" | Validated |
| RE-1 | Requirements Elicitation | Scope Boundaries | M | All 9 mitigation-plan finding areas are in scope for this work package — the plan is the agreed mitigation set and no finding area was explicitly excluded during elicitation | User (mitigation plan approved as the work-package scope) | Confirmed |
| RE-2 | Requirements Elicitation | Implicit Requirements | M | Backward compatibility is preserved where feasible — mitigations default to opt-in env gates rather than breaking the existing read/control surface; the useful read surface (OHLCV, quote, study values, pine reads) stays always-on | User (design-philosophy constraints: "backward-compatible where feasible; preserve the useful read/control surface") | Confirmed |
| RE-3 | Requirements Elicitation | Success Criteria Interpretation | L | Success is verified primarily by the existing `node --test` suite plus new unit tests for each gate/guard (the `_deps` injection points make them unit-testable); no live-CDP e2e is required to call the mitigation done | Code: `tests/{update,launch,sanitization}.test.js` exist; `_deps` injection present in `core/update.js`, `core/health.js` (guards unit-testable without CDP) | Validated |
| RE-4 | Requirements Elicitation | Implicit Requirements | L | Stakeholder discussion was skipped by the user in favor of agent-led elicitation — requirements derive from the security audit + mitigation plan + the three DP decisions, not from a separate stakeholder transcript | User (stakeholder-transcript checkpoint → skip-discussion) | Confirmed |
| RS-1 | Research | Pattern Applicability | M | The deny-by-default, registrar-enforced tool-allowlist pattern (from MCP gateway/proxy sources) applies to a single-process stdio MCP server even though sources frame it at a gateway/proxy layer — the enforcement point moves from a network gateway to the in-process `server.tool` registration funnel, preserving the structural property (agent cannot bypass) | Web: Securie/systemshardening/gethasp frame allowlists at a gateway; comprehension artifact confirms `server.tool` is the single in-process funnel | Validated |
| RS-2 | Research | Pattern Applicability | L | Spotlighting-style fencing (delimit/datamark/encode + treat-as-data instruction) reduces indirect prompt injection when applied to chart-derived tool output — sources measure XPIA success dropping from >50% to <2% on general models; assumes the chart text is the untrusted boundary and the model honors the fence | Web: Microsoft MSRC + arXiv 2403.14720 efficacy figures | Validated |
| RS-3 | Research | Source Relevance | L | The CI/dependency findings (SHA pinning, permissions, npm ci, failing audit, lockfile-lint) come from 2025-2026 sources that post-date relevant supply-chain incidents (e.g. Trivy compromise), so they reflect current best practice rather than stale guidance | Web: GitHub changelog 2025-08-15; Eclipse/Trivy 2026-03 postmortem; safeguard.sh/pkgpulse 2026 | Validated |
| RS-4 | Research | Synthesis Decisions | L | Where sources offer multiple strength levels (delimiter vs datamark vs encode for fencing; moderate vs high audit level), the synthesis recommends the stronger option the project's threat model warrants while noting the cheaper fallback — a synthesis judgement, not a direct source quote | Agent synthesis decision | Confirmed |
| RS-5 | Research | Risk Assessment | M | Research treats the allowlist/registrar — not fencing or a prompt-injection scrubber — as the trust boundary, with fencing as a complementary layer; a scrubber-only posture is an anti-pattern the sources explicitly warn against | Web: gethasp ("scrubber is not a trust boundary"); Securie (structural defense) | Validated |
| IA-1 | Implementation Analysis | Current Behavior | M | The 84 `server.tool` registrations across 14 registrars are all unconditional — verified by reading `src/server.js:73-86` and each registrar; there is no existing gate, so the gating change is purely additive (a name-set check) with no current behavior to preserve | Code: `rg "DANGEROUS_TOOLS|TV_ALLOW_DANGEROUS|wrapUntrusted|TV_UPDATE_TOKEN" src/` → 0 matches; `src/server.js:73-86` | Validated |
| IA-2 | Implementation Analysis | Dependency Understanding | M | `tv_update`'s trust gap is *what* is fetched, not the merge mechanics — `update()` guards (non-git, branch, dirty, ahead) are genuine, but `git fetch origin main` has no origin-URL allowlist, no signed-tag/pinned-SHA check, and no token; `npm ci` failure is warn-not-fail (`depsWarning`) leaving code/deps skew | Code: `src/core/update.js:23-104` | Validated |
| IA-3 | Implementation Analysis | Gap Identification | L | `kill_existing` default is declared only in the tool *description* text ("default true") while the zod schema leaves it `.optional()` — so flipping the default requires editing both the description and adding a schema `.default(false)`, and the MSIX fallback path calls `killExisting()` unconditionally regardless of the caller's flag | Code: `src/tools/health.js:24`; `src/core/health.js:352-360,376` | Validated |

| PL-1 | Plan & Prepare | Design Approach | M | A single `wrapRegistrar` interception point in `src/server.js` is sufficient enforcement for all 14 registrars — each registrar receives the wrapped server, so no per-registrar edits are needed and future registrars inherit the gate by construction | Agent design decision; registrar call-graph is flat (IA-1, comprehension artifact) | Confirmed — flat call graph verified at `src/server.js:73-86`; wrap applies uniformly |
| PL-2 | Plan & Prepare | Design Approach | M | Fencing string *values* only (never keys/structure) inside `jsonResult` preserves machine-parseability for downstream consumers while still marking untrusted content — assumes no consumer parses inside string values as commands | Agent design decision; recorded as plan risk with round-trip tests | Confirmed — value-only fencing specified; round-trip tests PR1-TC-05/06 will validate |
| PL-3 | Plan & Prepare | Dependency Assumptions | L | GPG signed-tag verification may be unavailable on some installs, so `tv_update` carries a `TV_UPDATE_PINNED_SHA` fallback that satisfies the same provenance intent — both paths fail closed | Agent design decision from research (fail-closed enforcement pattern) | Confirmed — fallback specified in plan Task 4 |
| PL-4 | Plan & Prepare | Scope Decisions | L | Windows MSIX local-copy hash verification stays out of scope (no Windows test env); the MSIX fallback is instead brought under the caller's `kill_existing` flag so it no longer kills unconditionally | Requirements out-of-scope item 1 | Confirmed — matches requirements Out of Scope #1 |
| AR-1 | Assumptions Review | Scope | L | No significant assumptions (all 22 prior rows validated/confirmed; the plan-approval gate introduced no new implicit decisions) | Agent review of the full log against the approved plan | Confirmed |
| IM-1 | Implement | Dependency Assumptions | L | The `security:audit` threshold (`--audit-level=high`) holds after `npm audit fix` resolves the fixable highs; the two residual **moderate** advisories inside `@modelcontextprotocol/sdk` (Hono) would require `--force` (a range-breaking upgrade) to clear, so they are accepted at the pinned version rather than chased — the audit gate stays green at high without force | Code: `npm audit fix` applied; `npm run security:audit` exits 0 (audit + lockfile-lint) with 2 moderate advisories remaining | Confirmed |

## Wrap-Up

23 assumptions through assumptions-review — 18 validated/confirmed, plan added 4 (PL-1..PL-4), review added a null row (AR-1). Implement added one dependency assumption (IM-1): residual *moderate* Hono advisories inside the pinned `@modelcontextprotocol/sdk` are accepted rather than resolved via a range-breaking `--force` upgrade, keeping the high-severity audit gate green. No open assumptions remain; `has_open_assumptions` is false and no interview is owed.
