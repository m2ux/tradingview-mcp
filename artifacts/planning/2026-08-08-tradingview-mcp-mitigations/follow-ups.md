# Follow-Ups

> TradingView MCP Security Mitigations · #none (repo issues disabled) · updated 2026-08-08

| ID | Surfaced at | Item | Owner / next step | Status |
|----|-------------|------|-------------------|--------|
| F-1 | strategic-review | `CSS.escape()`/`cssEscapeAttr()` for user-derived selector values (requirements in-scope #6; `ui_find_element` css-strategy guard PR1-TC-18 is a negative blacklist; whitelist escaping not implemented) | Agent + human decision at review-findings gate — fix in-place or defer to [deferred-items.md](deferred-items.md) | done (6915599) |
| F-2 | strategic-review | `package.json` `test` / `test:unit` / `test:all` / `test:cli` scripts omit `tests/capabilities.test.js`, `tests/fencing.test.js`, `tests/guards.test.js`, `tests/server-gating.test.js` — new suites run under bare `node --test tests/` only | Agent + human decision at review-findings gate — add the four files to the script lists | done (6915599) |
| F-3 | user review (2026-08-08) | Exploit-path E2E suite (`test:exploit`) — assert each mitigation blocks the real attack against a live/real target, not just guard logic via `_deps`. Current coverage proves "guard function returns the right answer for hostile input" but only the registrar gate is proven at integration level (`server-gating`); the rest (selector injection in a live page, `ui_evaluate` refused end-to-end, `tv_update` refusing a malicious real git remote, forged-fence survival in an actual MCP payload, exact-path kill against a live process tree) are unit-tested at the boundary only. Thorough scope: opt-in `tests/exploit/` gated behind env (never in CI), one live-target case per finding area. | folded → [13-follow-up-live-target-assurance.md](13-follow-up-live-target-assurance.md) Part A · start after PR #1 merges | planned |
| F-4 | user review (2026-08-08) | **API availability canary** (`test:canary`) — **on-demand** probe (user decision 2026-08-08: CI not practical — TradingView needs a live authenticated GUI session, can't run headless in GitHub CI). Run by hand or before a release against a live session to confirm the curated tool surface still matches TradingView's undocumented internals. Tool set is curated over `KNOWN_PATHS` (window.TradingViewApi.*); a TradingView update can silently break a probe path. Build: per-path assertions reusing `verifyAndReturn`/`tv_discover` mechanics; opt-in env gate + reachable CDP at 127.0.0.1:9222; skips cleanly otherwise. Two tiers: (a) structural — each KNOWN_PATHS entry resolves + expected methods present; (b) smoke — each read-only tool returns its documented shape. Read-only by construction; never in default suite. A curated manifest (`tests/canary/expected.json`) maps path→required methods and tool→required keys, updated in the same PR as any tool change. | folded → [13-follow-up-live-target-assurance.md](13-follow-up-live-target-assurance.md) Part B · start after PR #1 merges | planned |
| F-5 | user design discussion (2026-08-08) | **Custom drawing tools (channels/levels/rules) as discrete MCP tools.** User wants their custom channels/levels/drawing rules mapped to discrete tools on the existing application layer. **Recommendation: integrate into this project** (new `register*Tools` group + `src/core/*` module composed over the existing `createShape`/`createMultipointShape`/`getAllShapes` primitives in `src/core/drawing.js`) — an independent tool would sit outside the registrar gate/fencing/audit boundary. **Naming constraint:** MCP tool names are identifiers, not paths — `/area/tool` breaks the registrar's exact-name allowlist lookup (`capabilities.js` `GATED_TOOLS`/`REMOVED_TOOLS`) plus every doc/client reference; the flat `area_tool` prefix already encodes the area. Proposal: keep flat `_` names, add the custom drawings as a new prefix group (e.g. `channel_*`, `level_*`). **Deferred by user to a new issue:** (a) naming decision (flat-new-group vs `/area/tool` migration vs hybrid alias), (b) drawing scope (channels / levels / both / survey-first). | Human — open a dedicated issue/work package to decide naming + scope, then propose tools via the capability-allowlist PR path | open |
