# Assumptions Log

> Improve CDP Architecture of TradingView MCP Server · #24 · updated 2026-08-15

## Log

One row per assumption, updated in place. IDs: two-letter phase prefix + sequence
(DP-1, RE-1, RS-1, IA-1, PL-1) or task number (1.1, 2.3).

| ID | Phase/Task | Category | Risk | Assumption — rationale | Resolution | Outcome |
|----|------------|----------|------|------------------------|------------|---------|
| DP-1 | Design Philosophy | Workflow Path | M | Full-workflow path is warranted despite thorough prior research — the prior reports are read-only analysis of breaches/build order, not elicited requirements or a synthesized research base for this package, so elicitation + research add genuine value rather than duplicating it | User (classification-and-path-confirmed gate) | Confirmed |
| DP-2 | Design Philosophy | Complexity Assessment | M | `src/connection.js` blast radius (21 direct importers, HIGH) is representative of the whole change's complexity — used as the objective complexity signal; the actual blast radius across all five refactors (R1–R5) is larger but directionally consistent | Code: gitnexus impact on `src/connection.js` (21 d=1 IMPORTS, HIGH) | Validated |
| DP-3 | Design Philosophy | Problem Interpretation | L | Classified as inventive-improvement, not prevention — the 2026-08-14 connection wedges were a real symptom, but the issue frames the work as proactive improvement of a working channel, and no current outage/failure demands a specific-problem fix | User (classification-and-path-confirmed gate) | Confirmed |
| RE-1 | Requirements Elicitation | Requirement Interpretation | M | The issue's build order (R2 → R1 → R3 → R4 → R5) is read as the intended landing sequence, not merely a suggestion — the research presents each R as independently mergeable and the issue states the problem without prescribing solutions, so the order could be revisited | User (research-assumption-interview gate, batch-accepted 2026-08-15) | Confirmed |
| RE-2 | Requirements Elicitation | Scope Boundaries | M | Event-driven/push redesign and new-tool adoption are out of scope — research §6 names them follow-ups on the cleaned base, and the issue's title frames the package as architecture repair; a stakeholder could read "improve CDP architecture" as including the push direction | User (research-assumption-interview gate, batch-accepted 2026-08-15) | Confirmed |
| RE-3 | Requirements Elicitation | Implicit Requirements | M | Backward compatibility covers tool behaviour and the CLI, but not internal module paths — core module splits (R5) and moves (R3) may break deep imports by external consumers of `tradingview-mcp/core`; assumed acceptable because the facade is partial and the package's consumers are in-repo | Code: core/index.js exports 12 namespaces; cli/commands imports beyond them | Partially Validated |
| RE-4 | Requirements Elicitation | Success Criteria Interpretation | L | SC-7's "no TV_CDP_BUSY on a parallel-tab scenario" is a smoke-level bar, not a load-tested guarantee — live-Desktop verification is scenario-based; a statistical contention measurement is beyond this package's verification means | User (research-assumption-interview gate, batch-accepted 2026-08-15) | Confirmed |
| RE-5 | Requirements Elicitation | Implicit Requirements | L | The 88-tool count in server instructions is the compatibility baseline — tools may be neither added nor removed by the refactor; capabilities gating (TV_ALLOW_DANGEROUS) behaviour is preserved as-is | Code: server.js instructions string; capabilities.js wrapRegistrar | Validated |
| RS-1 | Research | Source Relevance | L | The concept-rag knowledge base's absence of CDP/Electron material does not invalidate the research base — the library is domain literature, not tooling docs; web sources plus the prior in-repo external research carry the package's evidence weight | Code: KB queries returned only database/pattern-catalog texts; prior research report covers the domain | Validated |
| RS-2 | Research | Pattern Applicability | M | A bounded N-socket pool (one private socket per background target, from a single factory) is sufficient for SC-7 — published limits (~30 WS/host, ~255 global in Chromium) sit far above observed usage, so the wedge is endpoint busy-behavior, not numeric exhaustion; assumed TradingView's endpoint behaves like the documented Chrome-family endpoints | Web: websocket.org connection-limits guide; Chromium socket-pool constants; flutter/devtools #8298 | Validated |
| RS-3 | Research | Synthesis Decisions | M | Flatten-session multiplexing (`Target.attachToTarget` + per-command sessionId) is recorded as a rejected path for this package and deferred to the event-driven follow-up — canonical upstream pattern, but a larger rewrite than R1–R5 requires and no success criterion demands it; hinges on RE-2's scope boundary holding | Web: chrome-remote-interface #531/#533; SO flatten-session answer | Confirmed-position (interacts with open RE-2) |
| RS-4 | Research | Risk Assessment | L | TradingView's proprietary devtools endpoint behaves like documented Chrome-family CDP endpoints on concurrent connections — no published source covers it, so R1 relies on the in-tree evidence (withTargetEvaluate retry/TV_CDP_BUSY machinery) plus general endpoint behavior; residual uncertainty is carried as research candidates C-1/C-2, not a blocking assumption | Code: connection.js withTargetEvaluate header + retry machinery | Partially Validated |
| IA-1 | Implementation Analysis | Dependency Understanding | L | The transport-provided scoped-client factory can subsume the three bypass patterns without unifying them: tab.js (shell-window target + post-op reconnectTo), capture.js (deliberately off the shared client mid-evaluate), withTargetEvaluate (background chart target + retry) differ in target type and lifetime, so the factory is parameterized per-site rather than forcing one idiomatic client | Code: tab.js:51/76/97, capture.js:17, connection.js:189 opens with distinct targets/lifetimes | Validated |
| IA-2 | Implementation Analysis | Baseline Interpretation | M | The 5 unit-run failures are live-dependency / stub-gated, not package-introduced: cli.test.js's pine_check uploads source to the real pine-facade endpoint (TV_ALLOW_PINE_CHECK_UPLOAD=1; test map :134-158) and fails exit 1 on endpoint unavailability — a live-network requirement orthogonal to the CDP refactor; e2e.test.js is a live-Desktop file that fails at harness level inside npm test's multi-file run; pine_workflow.test.js:35 carries the same network-gated check. Baseline = "no worse than 355/360 and the failure set must not grow" | Code: rg probe 2026-08-15 + cli.test.js:135-141 upload opt-in; branch is untouched scaffold at 4ff5104 | Validated |
| IA-3 | Implementation Analysis | Current Behavior | L | R4's per-site poll-vs-delay classification (research candidate C-3) is deferred to plan-time code analysis against the live UI: the decision rule itself is fixed (poll when a nameable condition exists, shared sleep otherwise) but applying it per call site is an implementation fact, not derivable from static analysis alone | Code: rg setTimeout per-module spread (46 non-wait sites); 04-kb-research.md C-3 handoff | Validated |
| IA-4 | Implementation Analysis | Current Behavior | L | R1's bounded-pool size/eviction (research candidate C-1) lands as plan-time defaults with retry/backoff kept as the safety net: TradingView's endpoint wedge-threshold is not published and is workload-specific, so the pool ships parameterized rather than "researched"; a bounded live experiment is optional verification, not a blocker | Code: websocket.org limits (research Web 3); connection.js retry machinery as safety net | Validated |
| IA-5 | Implementation Analysis | Gap Identification | L | health.js's three concerns split cleanly with no cross-calls — page probes (healthCheck/discover/uiState), process launch (kill/MSIX/spawn/CDP-wait), git update-check (+1 h cache) — so R5 is a pure module move needing no shared-helper extraction | Code: health.js:13/45/85/131/345 export map; 489-line layout | Validated |
| PL-1 | Plan & Prepare | Design Approach | M | The R1 pool ships as N-socket-per-target with default LRU-8, lifecycle-aware eviction, and preserved transient-retry/TV_CDP_BUSY semantics — the minimal departure from the existing per-target flow; alternatives: flatten-session multiplex (rejected per RS-3, deferred), open/close-per-call (the current wedge vector) | Code: RUDE-labs pool design (prior research 02 §3); connection.js:217 retry idiom reused | Validated |
| PL-2 | Plan & Prepare | Design Approach | L | Registry consumption consolidates onto the verified-getter idiom (`getChartApi()`/`getChartCollection()`) rather than map imports where a module doesn't already use the map — the getter carries call-time `verifyAndReturn` so SC-2's consolidation can't silently reintroduce unverified literals | Code: connection.js:283-308 getters; drawing/batch/replay already consume them | Validated |
| PL-3 | Plan & Prepare | Design Approach | L | R3's protocol home is a single designated module (`core/dom.js` extended in place preferred over a new file) that the existing DI seams can fake, rather than every caller continuing to hold raw clients — keeps Input/Photo call normalization in one file | Code: comprehension DI-seam layout (11 modules `_deps`); dom.js already holds Input idioms | Validated |
| PL-4 | Plan & Prepare | Task Breakdown | M | The five slices land as one commit-per-R-severable sequence (R2 → R1-listing+churn → R1-pool → R3 → R4 → R5) on one branch rather than five separate PRs — SC-8 demands independently mergeable slices, and commit-level severing carries a single PR's review cost; git-grand-counter tooling handles any re-splitting later if review prefers it | Code: SC-8 criteria; branch already at scaffold 4ff5104 | Validated |
| PL-5 | Plan & Prepare | Dependency Assumptions | L | sync-branch against origin/main is a no-op or trivial for this package (scaffold branch cut from current main within the day) so prerequisites only; deep-import breakage from R3/R5 moves remains acceptable per RE-3 with re-export shims preserved where cheap | Code: git worktree list (4ff5104 current); RE-3 row | Validated |
| PL-6 | Plan & Prepare | Test Strategy | M | SC-7 validation is a named manual scenario run on live Desktop only after the R1-pool slice lands — consistent with RE-4's smoke-level bar; unit tests carry no synthetic contention load-tests, matching community norm of symptom-level verification | Code: RE-4 row; 04-kb-research.md SC-7 row | Validated |
| PL-7 | Plan & Prepare | Scope Decisions | L | No new tools, manager surfaces, or push/event-driven capability are added even opportunistically: RE-2's scope boundary holds, so the plan's slices confine to R1–R5 repair plus compatibility and verification only | Code: requirements Out of Scope §1/§2; RE-2 row | Validated |
| IM-1 | Implement (R2) | Design Approach | L | KNOWN_PATHS registry scoped to `chartApi` and `chartCollection` only — these were the only `window.TradingViewApi._*` paths that appeared across the 10-module sweep; no other `._backend`/`._context` paths were found, so the registry stays minimal rather than speculative | Code: `rg "window\.TradingView\._" src/` outside connection.js returns zero hits post-sweep (4fa01b6) | Validated |
| IM-2 | Implement (R1-pool) | Design Approach | L | Pool eviction is LRU-8 with `TV_CDP_POOL_SIZE` env override and `drainScopedPool()` on `disconnect()` — no idle-timeout or max-age policy; the pool is process-scoped and drained on shutdown, so a time-based policy would add complexity without benefit | Code: connection.js `SCOPED_POOL_SIZE`/`_evictScoped`/`drainScopedPool` (a3aaf16) | Validated |
| IM-3 | Implement (R1-pool) | Current Behavior | L | `_makeScopedClientRaw` unconditionally enables `Runtime.enable()`, `Page.enable()`, `DOM.enable()` — matches the existing `withTargetEvaluate` pre-enable set; callers that don't need `DOM` pay a negligible one-time round-trip cost | Code: connection.js `_makeScopedClientRaw` mirrors prior `withTargetEvaluate` enables (a3aaf16) | Validated |
| IM-4 | Implement (R3) | Design Approach | L | Protocol module exports only multi-site CDP calls (`captureScreenshot`, `dispatchMouse`, `dispatchKey`, `insertText`); single-site CDP calls like `Page.navigate` (tab.js) and `Runtime.evaluate` (everywhere) were left in place because centralizing them would add indirection without consolidation | Code: protocol.js export map; `rg "Page\.|Input\.|Emulation\."` confirms multi-site coverage (015440a) | Validated |
| IM-5 | Implement (R4) | Design Approach | L | `sleep` helper placed in `wait.js` rather than a new `sleep.js` — it is a one-liner that belongs alongside the existing `waitFor*` family; a separate file would be over-engineering | Code: wait.js `export const sleep` (bfe7cf1) | Validated |
| IM-6 | Implement (R5) | Design Approach | L | Health split uses re-export shims (`export { launch } from './launch.js'`) to preserve `import { launch } from '../core/health.js'` paths used by `bin/tv-mcp.js` and `tests/launch.test.js` — cheaper than updating all import sites and maintains the facade per RE-3 | Code: health.js re-export shims; `rg "from.*health" bin/ tests/` confirms path preservation (d23d351) | Validated |

Resolution: how it was settled — `Code:` with file:line evidence, `User` (checkpoint or
interview), or `—` while open; implementation-task rows append the commit hash for
assumption-to-commit traceability. Outcome: Validated / Invalidated / Partially Validated
(code-resolved) · Confirmed / Corrected: <change> / Deferred: <follow-up> (user-resolved)
· Open (<reason>). When an interpretation difference contributed to an assumption, name
the ambiguity source (observation, recall, requirement reading, ambiguous problem
statement) in the rationale.

## Open Assumptions

None — all assumptions resolved. RE-1, RE-2, and RE-4 were batch-accepted at the research-assumption-interview gate (2026-08-15). IA-1..IA-5 resolved against code evidence during implementation analysis; PL-1..PL-7 resolved against code/research evidence during planning; IM-1..IM-6 resolved against committed code evidence during implementation. None stakeholder-dependent.

## Wrap-Up

30 assumptions — all validated/confirmed. RE-1, RE-2, RE-4 batch-accepted by the user (2026-08-15); RS-1..RS-4 code/web-resolved during research; IA-1..IA-5 code-resolved during implementation analysis; PL-1..PL-7 code/research-resolved during plan-prepare (pool form + default sizing, registry getter idiom, protocol home, commit-per-slice landing, sync-branch triviality, SC-7 manual-smoke strategy, no-scope-drift); IM-1..IM-6 code-resolved during implementation (KNOWN_PATHS scope, pool eviction, client enables, protocol module scope, sleep location, re-export shims). No open, corrected, invalidated, or deferred items.
